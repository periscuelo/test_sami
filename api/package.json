{
  "name": "api",
  "version": "1.0.0",
  "description": "API for Sami Test",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_ENV=dev&& set NODE_PATH=src&& nodemon server.js",
    "test": "set NODE_ENV=test&& set PORT=80&& set BASE_URL=http://localhost&& set NODE_PATH=src&& nyc mocha test/* --exit"
  },
  "author": "Ibrahim Brumate",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.2",
    "jwt-simple": "^0.5.6",
    "mongoose": "^5.9.9",
    "restify": "^8.5.1"
  },
  "devDependencies": {
    "mocha": "^7.1.1",
    "nodemon": "^2.0.3",
    "nyc": "^15.0.1",
    "supertest": "^4.0.2"
  }
}
